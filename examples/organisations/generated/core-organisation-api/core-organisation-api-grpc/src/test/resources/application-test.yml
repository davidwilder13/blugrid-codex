micronaut:
  application:
    name: organisation-grpc
  server:
    port: -1  # dynamically assign REST port
  grpc:
    server:
      port: 0  # dynamically assign gRPC port (critical for multiple instances)
  test:
    transactional: false

consul:
  client:
    registration:
      enabled: false

grpc:
  client:
    discovery:
      enabled: false
  data:
    enabled: false
    jpa:
      enabled: false
    flyway:
      enabled: false

datasources:
  default:
    enabled: false

hibernate:
  enabled: false

jpa:
  default:
    enabled: false
    entity-scan:
      enabled: false

consul:
  client:
    defaultZone: http://${CONSUL_HOST}:${CONSUL_PORT}

logger:
  levels:
    io.micronaut.discovery: TRACE
    io.micronaut.grpc: TRACE
